{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c \"npx prisma migrate deploy && node src/index.js\"",
    "restartPolicyType": "ON_FAILURE"
  }
}
