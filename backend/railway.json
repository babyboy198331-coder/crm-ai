{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
          "builder": "DOCKERFILE",
          "dockerfilePath": "Dockerfile"
    },
    "deploy": {
          "startCommand": "sh -c \"npx prisma db push --accept-data-loss --skip-generate && node src/index.js\"",
          "restartPolicyType": "ON_FAILURE"
    }
}
